name: Test Secrets Configuration

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Secrets
        run: |
          echo "EASYSCAN_URL: ${{ secrets.EASYSCAN_URL }}"
